package keeper

import (
	sdk "github.com/cosmos/cosmos-sdk/types"
)

func (k Keeper) CreatePoll(ctx sdk.Context) (uint64, error) {
	// validate message

	// create a new poll

	// add new poll to store

	// return poll id
	return 0, nil
}
